{
  "name": "zernio-cloudflare-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Zernio comment.received webhook for Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
